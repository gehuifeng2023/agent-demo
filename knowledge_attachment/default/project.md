# 问答智能体项目说明

本项目是一个基于 Go 语言实现的问答智能体示例项目。

系统主要包含以下模块：

1. HTTP 接入层
2. Agent 编排层
3. 意图识别模块
4. Prompt 模板模块
5. LLM 调用模块
6. 文档检索模块

用户输入问题后，系统会先判断问题类型，然后选择合适的处理逻辑。

如果问题需要基于知识回答，系统会从本地文档中检索相关内容，并将检索结果拼接到 Prompt 中，再调用大模型生成答案。